diff --git a/apps/storybook/stories/ReviewPR5303.stories.tsx b/apps/storybook/stories/ReviewPR5303.stories.tsx
new file mode 100644
index 00000000000..83ee26405d1
--- /dev/null
+++ b/apps/storybook/stories/ReviewPR5303.stories.tsx
@@ -0,0 +1,55 @@
+// Copyright (c) Meta Platforms, Inc. and affiliates.
+
+import {useState} from 'react';
+import type {Meta, StoryObj} from '@storybook/react';
+import * as stylex from '@stylexjs/stylex';
+import {Calendar, type ISODateString} from '@astryxdesign/core/Calendar';
+import {DateInput} from '@astryxdesign/core/DateInput';
+import {InternationalizationProvider} from '@astryxdesign/core/i18n';
+import {Heading, Text} from '@astryxdesign/core/Text';
+import {Timestamp} from '@astryxdesign/core/Timestamp';
+import {spacingVars} from '@astryxdesign/core/theme/tokens.stylex';
+
+const meta = {title: 'Review/PR 5303'} satisfies Meta;
+export default meta;
+type Story = StoryObj<typeof meta>;
+
+const DATE = '2026-08-22' as ISODateString;
+
+export const DateConsistency: Story = {
+  render: () => {
+    const [value, setValue] = useState<ISODateString | undefined>(DATE);
+    return (
+      <InternationalizationProvider locale="th-TH">
+        <section aria-label="th-TH date examples" {...stylex.props(styles.root)}>
+          <Heading level={2}>th-TH</Heading>
+          <DateInput
+            label="Selected date"
+            value={value}
+            onChange={setValue}
+            format="date_long"
+          />
+          <Text>
+            Timestamp:{' '}
+            <Timestamp value="2026-08-22T12:00:00Z" format="date_long" />
+          </Text>
+          <Calendar
+            mode="single"
+            value={value}
+            onChange={setValue}
+            focusDate="2026-08-01"
+          />
+        </section>
+      </InternationalizationProvider>
+    );
+  },
+};
+
+const styles = stylex.create({
+  root: {
+    display: 'flex',
+    flexDirection: 'column',
+    gap: spacingVars['--spacing-4'],
+    maxWidth: 520,
+  },
+});
